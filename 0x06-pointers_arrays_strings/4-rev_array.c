#include "main.h"
/**
 * reverse_array - reverse array of the integers
 * @a: array
 * @n: number of the elements o the  array
 *
 * Return: void
 */
void reverse_array(int *a, int n)
{
	int z;
	int b;

	for (z = 0; i < z--; i++)
	{
		b = a[z];
		a[z] = a[n];
		a[n] = b;
	}
}

